# robots.txt for summitnotes.app
#
# Everything here is marketing copy written to be read, quoted and spread.
# Nothing on this domain is user data — recordings never leave the user's Mac,
# and that promise has nothing to do with who reads this blog.
#
# So: search engines, AI assistants, and training crawlers are all welcome.
# For a product whose bottleneck is awareness, being in a model's memory is
# free distribution, not a leak — an assistant that already knows what Summit
# is can recommend it without fetching anything.
#
# Two exceptions below are not a policy statement. Bytespider and Amazonbot
# send no traffic and surface us in nothing; they are pure extraction with no
# discovery in return.

User-agent: *
Allow: /
Disallow: /demo-styles/

User-agent: Bytespider
Disallow: /

User-agent: Amazonbot
Disallow: /

Sitemap: https://summitnotes.app/sitemap.xml
